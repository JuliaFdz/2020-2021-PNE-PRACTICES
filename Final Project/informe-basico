<!DOCTYPE html>
    <html lang="en" dir="ltr">
      <head>
        <meta charset="utf-8">
        <title>Browsing Human and Vertebrate Genome</title>
      </head>
      <body style="background-color: peachpuff;">
        <h1>Browsing Human and Vertebrate Genome</h1>
        <h2>Basic Level Services</h2>
        <font face="Comic Sans MS,arial">
        <form action="list" method="get"><br>
          1. List of species in the genome database:<br>
          Please select a limit:
          <select name="list">
          {% for n in range(0,context.n_species) %}
          <option value="{{n}}" >{{n}}</option>
          {% endfor %}
           </select>
          <input type="submit" value="Send">
            <font size="1">
              (Being 0  the initial number)
              </font>
                </form>
        <hr><br>
        <form action="karyotype" method="get">
          2. Information about the Karyotype: <br>
          Choose a Specie:
           <select name="karyotype">
          {% for n in context.list_species %}
              <option value="{{n}}" >{{n}}</option>
          {% endfor %}
          </select>
          <input type="submit" value="Send"> <br>
                  </form>

        <hr><br>
        <form action="length" method="get">
          3. Chromosome Length: <br>
            Select the Specie:
            <select name="election">
          {% for n in context.list_species %}
              <option value="{{n}}" >{{n}}</option>
          {% endfor %}
          </select>
            Select the Chromosome:
              <input type="text" name="msg">
              <input type="submit" value="Send"><br>
          </form>
            </font>
      </body>
    </html>
 prueba 1 _-- quitar errores, facilitando el acceso
